export type Student = {
  born: string;
  gender: string;
  grade: string;
  name: string;
  nisn: string;
  verval_id: string;
  profile: {
    address: string;
    nis: string;
    grade: string;
  };
};
